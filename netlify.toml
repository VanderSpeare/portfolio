[build]
  command   = "npm run build"
  publish   = "dist"

# Redirect all routes to index.html so React Router / hash links work
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
